# dependencies
node_modules/

# build output
dist/

# env
.env
.env.local
.env.*.local

# editor
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# logs
*.log
npm-debug.log*

# vite
*.local
.vercel
.env*

# graphify 생성물 — 용량 커서 git 제외, 노트북에서는 'graphify . --code-only'로 각자 재생성
graphify-out/
.claude/settings.json.graphify-bak

# 일회성 조사 산출물 (Supabase 사용량 점검 등) — 코드가 아니라 그때의 결과값
_supa_usage.json

# 빌드 캐시 — 기계마다 달라 충돌만 만든다
.vite/
