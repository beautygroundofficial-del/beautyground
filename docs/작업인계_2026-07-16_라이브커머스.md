# 작업 인계 — 라이브 커머스 (2026-07-16)

> 사무실 컴에서 Claude Code로 이 작업을 이어갈 때, 이 문서를 먼저 읽게 하세요.
> 첫 프롬프트 예시: `docs/작업인계_2026-07-16_라이브커머스.md 읽고 라이브 커머스 작업 이어가줘`

## 오늘까지 완료된 것 (전부 배포·검증됨)

1. **거짓 LIVE 수정** (커밋 b2d63db, 2a4f9d2)
   - Cloudflare `lifecycle` 폴링(공개, CORS *)으로 실송출 상태를 홈띠/라이브목록/시청화면에 반영
   - 송출 끊김 → "준비중" 배지 + 시청화면 "방송 준비 중" 대기 → 연결 감지 시 ~10초 내 자동 LIVE/재생
   - 핵심 파일: `src/hooks/useStreamStatus.ts`, `src/components/live/LiveStatusBadge.tsx`,
     `src/pages/app/ShopLiveWatch.tsx`, `src/components/home/HomeBody.tsx`
2. **송출 채널 자동 발급** (`api/live-input.ts` 신규)
   - POST: Cloudflare Live Input 생성(자동녹화 automatic) + `lives.stream_uid` 저장 / GET: RTMPS 주소·스트림키 조회
   - 파트너 방송화면(`src/pages/partner/LiveDetail.tsx`)에 "송출 채널" 섹션: 채널 만들기 버튼, 주소·키 복사, 연결상태(5초 폴링)
   - 스트림 키는 DB에 저장하지 않음(lives는 공개 테이블 — 방송탈취 방지). 매번 서버에서 조회
   - 인증: Supabase 액세스 토큰 → 파트너 본인(lives.partner_id → partners.user_id) 확인
3. Cloudflare 키 발급 완료 + 검증 통과 (계정ID `c21ea6ae9f29c52a51a80881228dd8cd`, 토큰은 대표님 보관 — 채팅 기록/집컴 `.env` 맨 아래. **깃에는 절대 커밋 금지**)

## 지금 막혀 있는 것 (다음 작업 시작점)

- **Vercel 환경변수 등록 대기**: `CLOUDFLARE_ACCOUNT_ID` / `CLOUDFLARE_API_TOKEN`을 홈페이지 프로젝트에 넣고 Redeploy 필요
  - ⚠️ 홈페이지(beautyground.vercel.app) Vercel 프로젝트는 **beautygroundofficial-del 계정에 없음** — 옛 베르셀 계정(추정: 깃허브 bageul88-hash 로그인) 소속. 대표님이 그 계정 로그인부터 찾아야 함
  - ⚠️ `vercel link` CLI 금지 — beautygroundofficial-del 팀에 빈 프로젝트를 새로 만들어버림(한 번 발생, API로 삭제함). 배포는 git push로만
  - 등록 안 된 동안에도 안전: 채널 만들기 버튼이 안내 문구로 우아하게 실패함
- 환경변수 등록 후 검증 절차: test3@test.com/test1234 로그인 → /partner/live/<id> → "송출 채널 만들기" → RTMPS 주소·키 표시 확인 → (가능하면) 폰 Larix로 송출 → 소비자 화면 LIVE 자동 전환 확인

## 그 다음 순서 (사용자와 합의된 로드맵)

1. **라이브 구매 실결제 연결** — 현재 ShopLiveWatch 구매 모달은 결제 없이 orders에 `status:'paid'` 직접 insert(가짜, 위험). 대표님이 PortOne 키 등록하면 기존 장바구니→AppOrder→PortOne 흐름에 `live_id` 태깅으로 연결하고 가짜 흐름 제거
2. "지금 판매" 하이라이트·공지핀을 시청자 화면에 실시간 동기화(현재 판매자 로컬에만 동작)
3. 다시보기(자동 녹화는 이미 켜서 발급됨 — 종료된 라이브에 녹화영상 연결)
4. 방송 통계(시청자수/판매액 placeholder)

## 검증 데이터 참고

- 기존 라이브 2건: "녹는실…"(status=live, uid=9d463006…, 송출 끊김→준비중 표시됨), "트리플…"(ended)
- 검증 스크립트: 집컴 `바탕화면\뷰티그라운드_작업자료\verify-live-*.mjs` (Playwright, BASE 환경변수로 로컬/운영 전환)
- Supabase: lives / chat_messages 테이블 사용 중. 재생 서브도메인: `src/lib/cloudflare.ts`
